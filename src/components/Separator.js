import React from 'react'

// this goes under the navbar so the canvas never starts behind the blur
const Separator = () => {
  return (
    <div className='flex p-7'></div>
  )
}

export default Separator